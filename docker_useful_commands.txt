sudo docker-compose run web django-admin startproject mysite . # to create root folder/directory in django docker
docker-compose up #need docker-compose.yml file to use
docker ps # when need to know process or host running.
sudo chown -R $USER:$USER . # to change root user to current use in ubuntu. b/c sometimes permission needed.
# to execute django commands while running docker.
docker exec django-docker_web_1 python manage.py startapp app

